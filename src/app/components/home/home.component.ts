import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  standalone: true,  // Componente independiente
  templateUrl: './home.component.html',  // Vínculo al archivo HTML
  styleUrls: ['./home.component.css']   // Vínculo al archivo CSS
})
export class HomeComponent {}
